import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  horizontal: { height: StyleSheet.hairlineWidth, alignSelf: 'stretch' },
  vertical: { width: 1, alignSelf: 'stretch', minHeight: 16 },
});
